//
//  ContentView.swift
//  RawDragAndDropProj
//

import SwiftUI

struct NotesListView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        
        }
        .padding()
    }
}

#Preview {
    NotesListView()
}
